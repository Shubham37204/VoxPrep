# Secrets — NEVER bake into image
.env
.env.*
*.env

# Version control
.git
.gitignore
.gitattributes

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Virtual environments — never copy into image
.venv/
venv/
env/
ENV/

# Test + validation — not needed in prod image
tests/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.log

# Build artifacts
dist/
build/
*.egg-info/
*.egg

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Docker (don't recurse)
Dockerfile
docker-compose*.yml
.dockerignore

# Alembic versions are needed — do NOT exclude alembic/
# Only exclude the compiled cache
alembic/__pycache__/